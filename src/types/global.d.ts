// Global type declarations for the Timetable Weaver application

// Declare types for any global objects or interfaces here if needed
// For now, we don't need any global declarations as we're using module imports
